{
  "name": "splitpath",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "Read-only split-DNS and private reverse-proxy diagnostics for self-hosters.",
  "scripts": {
    "test": "python3 -m compileall ."
  }
}
